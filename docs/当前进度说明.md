# Adapter MVP 当前进度说明

## 0. 总览表

| 模块 | 当前状态 | 已完成 | 待补/下一步 | 详细说明 |
| --- | --- | --- | --- | --- |
| 远端部署 | Done | 已部署到 `47.116.102.238:18080`，systemd 托管，审计日志落盘 | 持续维护环境变量和日志 | 2.1 |
| 鉴权和安全门禁 | Done | Bearer Token、preview/execute 审批、安全脱敏已具备 | 后续按接口增加更细权限 | 2.2 |
| Adapter Preview/Execute/Status/Audit | Done | 已支持统一 Adapter API 和 `ssh.check_connectivity` | 后续按需扩展更多工具动作 | 2.3 |
| MySQL 状态/审计/workflow 账本 | Done | `adapter_status`、`adapter_audit`、`adapter_workflow_instance`、`adapter_workflow_event` 已落地 | 继续补云效工作项和关单字段写入 | 2.4 |
| 钉钉/Alidocs 读取 | Done | 已通过 Adapter 读取钉钉需求文档，并接入 workflow | 后续固化 Codex 结构化解析协议 | 2.5、3.2 |
| Workflow P0 | Done | start、advance、requirement、coding-result、get workflow 已完成 | 后续补 retry/resolve 和更完整状态约束 | 2.6、4.1 |
| 云效回调底座 | Partial | task callback、flow-event callback、public/private 边界已具备 | 云效工作项创建/关闭还未打通 | 2.7、4.4 |
| 流水线失败分析 | Partial | 已有规则型失败分析器 | 后续补自动修复请求、retry、人工处理 | 2.8、4.8 |
| Apifox 自动导入 | Done | 已推送到 Apifox 项目 `8460173`，接口中文名已生效 | 缺少项目映射时明确报错，不再静默默认项目 | 2.9 |
| 流水线成功绑定 workflow | Done | Apifox 手工 1-6 流程已跑通，终点 `APIFOX_SYNCED` | 后续补无 `WORKFLOW_ID` 时的兜底匹配和 running 事件 | 4.6 |
| 主流程落地方案 | Done | 已补充每一步的推荐方案、取舍依据、外部接口和文档链接 | 后续实现必须按方案矩阵推进 | [主流程落地方案](/Users/jzm/Documents/develop/user-home/workspaces/adapter-mvp/docs/主流程落地方案.md) |
| 云效工作项创建 | P1 已实现，待联调 | 已按 HTTP/OpenAPI 接入 `CreateWorkitemV2`，并新增 `adapter_yunxiao_account_config` / `adapter_yunxiao_project_config` / `adapter_yunxiao_project_member` 支持 AK、多项目和人员默认负责人；可从 `REQUIREMENT_PARSED` 推进到 `CODING_REQUESTED`，缺配置会明确失败并记录事件 | 入库真实云效 AK、组织、项目、工作项类型和人员默认负责人后做真实联调 | 4.4、8 |
| 云效工作项关闭/回写 | P1 待实现 | 已完成 HTTP OpenAPI 调研方案，入口状态已有 `APIFOX_SYNCED` | Apifox 成功后评论/关闭云效任务并推进 `YUNXIAO_TASK_CLOSED` | 4.7、8 |
| 后续配置清单 | Done | 已集中整理云效、Apifox、钉钉、流水线和运行环境配置 | 后续新增配置先补清单，再改实现 | [配置清单](/Users/jzm/Documents/develop/user-home/workspaces/adapter-mvp/docs/后续配置清单.md) |
| Skill/MCP/Plugin 化 | P3 未完成 | 方向已明确 | 等主链路稳定后再做 Codex 调用体验增强 | 6 |

当前最关键的下一步：

```text
云效工作项创建真实联调
  -> 云效工作项关闭/回写
```

下面章节是每个模块的具体进度、流程和待办说明。

## 0.1 规划基线

这份文档同时承担两件事：

- 记录 `adapter-mvp` 当前已经完成了什么。
- 作为后续执行的总体规划基线。

具体落地方案见：[主流程落地方案](/Users/jzm/Documents/develop/user-home/workspaces/adapter-mvp/docs/主流程落地方案.md)。

后续需要补齐或迁移时容易漏掉的配置统一维护在：[Adapter MVP 后续配置清单](/Users/jzm/Documents/develop/user-home/workspaces/adapter-mvp/docs/后续配置清单.md)。

该方案已经把每一步绑定到推荐实现、方案取舍、外部系统接口、对应任务文档和验收标准。后续实现应先看这份方案，再进入具体任务文档。

后续开发默认按这里的主链路推进：

```text
钉钉需求文档
  -> 创建 workflow
  -> 读取钉钉文档
  -> Codex 解析需求
  -> 创建云效工作项
  -> Codex coding
  -> 提交 coding result
  -> 云效流水线回调
  -> 同步 API 到 Apifox
  -> 关闭/回写云效工作项
```

当前已经完成到：

```text
钉钉需求文档
  -> 创建 workflow
  -> 读取钉钉文档
  -> Codex 解析需求
  -> 创建云效工作项（代码和 DB 配置模型已实现，待真实云效配置联调）
  -> 提交 coding result
  -> 云效流水线回调
  -> 同步 API 到 Apifox
  -> APIFOX_SYNCED
```

接下来只围绕两个 P1 缺口推进：

```text
云效工作项创建真实联调
云效工作项关闭/回写
```

除非明确调整规划，否则不要优先做以下事情：

- 新增大而全的 Agent 框架。
- 先做 Skill/MCP/Plugin 化。
- 扩展无关 Adapter 动作。
- 重构已经跑通的 Apifox / workflow P0 主链路。

如果后续发现新需求，应先判断它属于上面哪一段链路；不属于主链路的，先放到 P2/P3，避免主流程跑偏。

## 1. 当前定位

`adapter-mvp` 目前已经不是单纯的 SSH 小工具，而是一个正在成型的交付集成服务。

当前更准确的定位是：

```text
Adapter MVP = 工具适配层 + 审计状态层 + 云效/钉钉/Apifox 集成入口
```

目标形态是：

```text
Adapter MVP = Workflow Orchestrator + Tool Adapter
```

也就是：Codex 负责理解需求和写代码，`adapter-mvp` 负责读钉钉、接云效、同步 Apifox、记录状态和审计。

## 2. 已完成能力

### 2.1 远端部署

已部署到远端服务器：

```text
公网地址：http://47.116.102.238:18080
应用目录：/opt/adapter-mvp
systemd 服务：adapter-mvp
环境变量：/etc/adapter-mvp/adapter-mvp.env
审计日志：/opt/adapter-mvp/logs/audit.jsonl
```

已具备：

- `/health` 探活接口。
- systemd 托管启动。
- 远端状态、重启、日志查看脚本。
- 密钥放在远端 env 和本地 `secrets/`，未放入代码仓库。

### 2.2 鉴权和安全门禁

已完成：

- 业务接口使用 `Authorization: Bearer <ADAPTER_API_TOKEN>`。
- `/health` 不需要 token。
- `/adapter/execute` 必须带 `approvalId` 或 `approved=true`。
- 云效 execute 回调同样需要审批参数。
- 审计和响应中避免输出密码、token、私钥。

当前安全策略是对的：默认 preview，execute 必须显式审批。

### 2.3 Adapter Preview / Execute / Status / Audit

已完成统一 Adapter API：

```http
POST /adapter/preview
POST /adapter/execute
GET  /adapter/status/{task_id}
GET  /adapter/audit/{task_id}
```

当前已支持动作：

```text
ssh.check_connectivity
```

用途：

- preview：只预览动作，不执行真实远端操作。
- execute：执行动作，但必须经过审批。
- status：查询执行结果。
- audit：查询审计事件。

### 2.4 MySQL 状态、审计和 workflow 持久化

已完成基础状态、审计和 workflow 账本表：

```text
adapter_status
adapter_audit
adapter_workflow_instance
adapter_workflow_event
```

能力：

- `adapter_status` 保存任务执行结果。
- `adapter_audit` 保存 preview、execute、status、pipeline failure、apifox import 等事件。
- `adapter_workflow_instance` 保存一次需求交付的主状态、钉钉入口、代码结果和后续云效/Apifox 绑定字段。
- `adapter_workflow_event` 保存 workflow 状态变化和关键事件。
- 审计同时写文件和 MySQL。
- 服务重启后，状态可从 MySQL 恢复。

这部分已经是后续云效工作项、流水线、Apifox、关单闭环的基础账本。

### 2.5 钉钉 / Alidocs 文档读取

已完成接口：

```http
POST /adapter/dingtalk/read
POST /adapter/dingtalk/config
POST /adapter/dingtalk/resolve-operator
```

当前能力：

- 通过 Adapter 托管的钉钉 OpenAPI 配置读取 Alidocs。
- 支持从 URL 提取 `nodeId`。
- 支持 `adoc` 文档。
- 支持 `axls` 表格，默认读取 `A1:J50`。
- 钉钉应用凭据和文档 endpoint 模板拆成两类配置。
- access_token 有缓存。
- 支持 operator 解析。

相关表：

```text
adapter_dingtalk_app
adapter_dingtalk_doc_config
adapter_dingtalk_app_config  # 旧兼容表
```

这部分已经可以作为 Codex 读取钉钉需求文档的工具入口。

### 2.6 Workflow P0 账本

已完成 workflow P0：

```http
POST /workflow/start
GET  /workflow/{workflow_id}
POST /workflow/{workflow_id}/advance
POST /workflow/{workflow_id}/requirement
POST /workflow/{workflow_id}/coding-result
```

当前能力：

- `/workflow/start` 从钉钉/Alidocs URL 创建 workflow 实例，状态为 `CREATED`。
- `/workflow/{id}/advance` 在 `CREATED` 状态读取钉钉文档，并推进到 `DOC_READ`。
- 钉钉读取失败会写 `doc_read_failed` 事件，状态保持 `CREATED`，便于修复配置后重试。
- `/workflow/{id}/requirement` 保存 Codex 结构化需求，并推进到 `REQUIREMENT_PARSED`。
- `/workflow/{id}/coding-result` 保存 branch、commit、MR、测试结果，并推进到 `CODE_SUBMITTED`。
- `GET /workflow/{id}` 返回实例和最近 workflow 事件。

这表示 P0 已经从“设计”变成“已落地的基础账本”。后续重点不再是建账本，而是把云效工作项、流水线和 Apifox/关单尾段接到账本上。

### 2.7 云效回调接入

当前云效只能算“部分接入”，不能算完整打通。

已实现的服务端入口：

```http
POST /callbacks/yunxiao/task
POST /callbacks/yunxiao/pipeline-failure
POST /callbacks/yunxiao/flow-event
POST /callbacks/yunxiao/flow-event/public
```

已具备的能力：

- 云效任务回调可触发 Adapter preview。
- 云效任务回调可在带审批时触发 execute。
- 云效流水线失败可调用 `pipeline_agent.py` 做规则分析。
- 云效通用 flow event 可以识别成功、失败、取消等状态。
- public flow event 路由已保留给云效无法带认证头的场景。

已经验证过的部分：

- 云效 Preview 节点调用成功。
- 云效 Audit 节点能查到同一个 `TASK_ID` 的审计记录。
- Preview / Audit 拆分节点已验证成功。

尚未完整打通的部分：

- Adapter 主动创建云效工作项。
- Adapter 主动关闭云效工作项。
- 云效工作项状态和 workflow 状态双向绑定。
- 云效流水线成功后精确匹配某个 workflow 实例。
- 云效成功后自动执行“Apifox 同步 -> 云效关单”的闭环。

所以当前结论是：云效到 Adapter 的低风险 Preview/Audit 链路已验证，云效工作项和完整交付闭环还没打通。

### 2.8 流水线失败分析

已有 `app/pipeline_agent.py`，当前是规则型分析器。

可识别：

- 编译失败。
- 测试失败。
- 依赖解析失败。
- Adapter 执行失败。
- 质量门禁失败。

它现在还不是完整 Agent，更像一个轻量规则分析器。这个阶段够用，后面可以升级成更智能的失败分析 Agent。

### 2.9 Apifox 自动导入

已完成核心逻辑：

- 云效 flow event 成功时，可触发 `maybe_import_from_flow_event`。
- 可按 pipelineId / projectName / repo / 环境变量解析 Apifox 项目。
- 支持数据库配置：

```text
adapter_apifox_project_config
adapter_apifox_pipeline_config
```

已支持：

- 拉取上游 OpenAPI。
- 处理被 JSON 字符串或 base64 包裹的 OpenAPI 返回。
- 校验 OpenAPI 基础结构。
- 去掉项目名前缀路径。
- 通过 Adapter 暴露清洗后的 OpenAPI：

```http
GET /adapter/openapi/{project_name}
```

注意：Apifox 自动导入是否真的执行，受环境变量控制：

```text
APIFOX_AUTO_IMPORT=true
APIFOX_ACCESS_TOKEN=...
APIFOX_PROJECT_ID=...
```

没有配置完整时，会返回 skipped，不会强行导入。

### 2.10 测试覆盖

当前已有测试：

```text
tests/test_dingtalk_split_config_mapping.py
tests/test_dingtalk_operator_resolution.py
tests/test_public_flow_event_route.py
tests/test_apifox_openapi_wrapped_response.py
tests/test_workflow_p0.py
```

覆盖点包括：

- 钉钉配置拆分映射。
- 钉钉 operator 解析。
- flow event public/private 路由安全边界。
- Apifox OpenAPI 包裹响应解析。
- workflow P0 的启动、钉钉读取推进、失败记录、结构化需求提交、coding 结果提交。

## 3. 当前大概流程

### 3.1 已经跑通的实际流程

```text
云效流水线 / 人工调用
  -> /callbacks/yunxiao/task
  -> Adapter preview
  -> 写 audit
  -> /adapter/audit/{task_id} 查询
```

这条链路已验证，但它只代表云效能调用 Adapter 并完成 Preview/Audit，不代表云效任务创建、流水线成功闭环、关单已经完成。

### 3.2 当前已接入 workflow P0 的钉钉需求流程

```text
Codex / 调用方
  -> /workflow/start
  -> /workflow/{id}/advance
  -> Adapter 读取钉钉需求文档
  -> 保存到 workflow context
  -> Codex 解析需求
  -> /workflow/{id}/requirement
```

这条链路已完成 P0：钉钉读取结果会进入 `adapter_workflow_instance.context_json`，并写入 `adapter_workflow_event`。

### 3.3 当前云效成功后同步 Apifox 的流程

```text
云效 flow event 成功
  -> /callbacks/yunxiao/flow-event 或 /public
  -> maybe_import_from_flow_event
  -> 解析 pipeline/project/openapi/apifox 配置
  -> 可选导入 Apifox
  -> 写 apifox_import audit
```

这段已经有实现，但目前还是事件级工具链，不是完整业务闭环：

- 已能根据云效成功事件尝试解析项目和 Apifox 配置。
- 已能在配置完整时触发 Apifox 导入。
- 已能在事件携带 `WORKFLOW_ID` / `workflowId` 时绑定 `adapter_workflow_instance`。
- 已能在 workflow 事件表记录 `PIPELINE_SUCCESS -> APIFOX_SYNCED`。
- 还不能在 Apifox 成功后自动关闭云效工作项。
- 还没有实现 `APIFOX_SYNCED -> YUNXIAO_TASK_CLOSED` 的自动关单链路。

### 3.4 目标完整流程

```text
钉钉需求文档
  -> adapter-mvp 创建 workflow
  -> adapter-mvp 读取钉钉文档
  -> Codex 解析需求
  -> adapter-mvp 创建云效任务
  -> Codex coding
  -> Codex 提交 branch/commit/MR
  -> 云效流水线运行
  -> 云效成功回调 adapter-mvp
  -> adapter-mvp 同步 API 到 Apifox
  -> adapter-mvp 关闭云效任务
```

这条完整链路还没有全部实现。当前已经完成的是工具底座、回调底座、从钉钉需求入口到 `CODE_SUBMITTED` 的 workflow P0 账本，以及流水线成功回调到 `APIFOX_SYNCED` 的绑定链路。

## 4. 尚未完成的能力

### 4.1 Workflow P0 已完成，后续状态机仍需扩展

已完成设计文档：

```text
docs/工作流编排设计.md
```

当前已实现 P0：

- `adapter_workflow_instance`
- `adapter_workflow_event`
- `/workflow/start`
- `/workflow/{id}`
- `/workflow/{id}/advance`
- `/workflow/{id}/requirement`
- `/workflow/{id}/coding-result`

其中 `/workflow/{id}/advance` 已支持从 `CREATED` 读取钉钉文档并推进到 `DOC_READ`；读取失败会记录 `doc_read_failed` 事件和 `last_error`，保留在 `CREATED` 便于修复配置后重试。

后续仍需补：

- `/workflow/{id}/retry`
- `/workflow/{id}/resolve`
- 更完整的状态迁移约束。
- 云效工作项、流水线、Apifox 同步结果、关单结果对 workflow 字段和事件的写入。

### 4.2 钉钉需求到 workflow 的基础绑定已完成

当前已支持：

- `/workflow/start` 自动创建 workflow 实例。
- 保存 `dingtalkUrl`、`nodeId` 到 `context_json`。
- `/workflow/{id}/advance` 读取钉钉文档。
- 将读取结果摘要保存到 `context_json.dingtalk.read`。
- 写入 `doc_read` / `doc_read_failed` 事件。

后续还需要给 Codex 返回更标准化的 `codingRequest`。

### 4.3 需求解析协议已具备 P0，Agent 自动化未固化

现在已经有 `/workflow/{workflow_id}/requirement` 作为结构化提交入口，字段包括：

```text
summary
acceptanceCriteria
affectedRepos
apiChanges
testScope
risk
openQuestions
extra
```

未固化的是：Codex 读取钉钉文档后，如何稳定地产出这份 JSON，以及是否做成 Skill / MCP / Plugin。

### 4.4 云效工作项创建/关闭未实现

现在已有云效回调入口，也有 `scripts/yunxiao_workitem_writeback.sh` 这种脚本级尝试，但还没有产品化的云效工作项 adapter。

已完成 HTTP OpenAPI 调研方案：

```text
docs/云效开放接口调研方案.md
```

已拆成两个任务：

```text
docs/云效工作项创建任务.md
docs/云效工作项关闭和回写任务.md
```

需要补：

- 云效 OpenAPI 配置。
- 创建主任务接口。
- 更新任务状态接口。
- 关闭任务接口。
- 评论/回写接口。
- 统一封装到服务端，而不是只靠云效 Shell 脚本。
- 将云效工作项 ID 写入 workflow 实例。

### 4.5 Coding 结果回传 P0 已实现

当前已有固定入口：

```http
POST /workflow/{workflow_id}/coding-result
```

保存：

- 分支名。
- commitId。
- MR 地址。
- 变更摘要。
- 测试结果。

状态会推进到 `CODE_SUBMITTED`。后续还需要接云效流水线运行状态。

### 4.6 流水线成功回调与 workflow 绑定已完成

当前已完成第一版绑定，并已通过 Apifox 手工跑通 `workflow` 1-6 流程：

- 云效成功/失败事件携带 `WORKFLOW_ID` / `workflowId` 时，可以绑定到 workflow。
- 成功事件会把 `CODE_SUBMITTED` 或 `PIPELINE_RUNNING` 推进到 `PIPELINE_SUCCESS`。
- Apifox 返回 `imported=true` 时，会继续推进到 `APIFOX_SYNCED`。
- Apifox skipped/failed 时，状态保留在 `PIPELINE_SUCCESS`，并记录 `apifox_sync_skipped` 或 `apifox_sync_failed` 事件。
- 失败事件会把 `CODE_SUBMITTED`、`PIPELINE_RUNNING` 或 `PIPELINE_SUCCESS` 推进到 `PIPELINE_FAILED`。

已验证的主链路：

```text
/workflow/start
  -> /workflow/{id}/advance
  -> /workflow/{id}/requirement
  -> /workflow/{id}/coding-result
  -> /callbacks/yunxiao/flow-event/public
  -> Apifox import-openapi
  -> APIFOX_SYNCED
```

还没有做的增强：

- 不带 `WORKFLOW_ID` 时按 `yunxiao_task_id`、`pipelineId + buildNumber`、`branchName + commitId` 兜底匹配。
- `PIPELINE_RUNNING` 的 running 状态事件处理。
- `APIFOX_SYNCED` 后自动关闭云效任务。

已拆成任务：

```text
docs/云效流水线打通任务.md
```

### 4.7 Apifox 同步后关闭云效任务未完成

Apifox 导入逻辑已有，但导入成功后还没有自动关闭云效任务。

需要等云效工作项 adapter 完成后接上。

### 4.8 失败恢复机制未完成

已有失败分析，但还没有完整恢复链路：

- `PIPELINE_FAILED` 状态。
- 自动生成修复请求。
- `NEEDS_HUMAN` 人工处理。
- 手动 retry / resolve。
- 最大重试次数控制。

### 4.9 Codex 自动调用 Adapter 的入口未固化

现在 Codex 可以手动调用 Adapter，但还没有形成稳定入口：

- Codex Skill。
- MCP tool。
- 或 Codex plugin。

建议先做 Skill 或 MCP tool，不急着做复杂 plugin。

## 5. 建议下一步怎么做

### Step 1：先实现被动 workflow 账本（已完成 P0）

目标：先让每个需求有一条主线记录。

新增：

- `adapter_workflow_instance`
- `adapter_workflow_event`
- `/workflow/start`
- `/workflow/{id}`
- `/workflow/{id}/requirement`
- `/workflow/{id}/coding-result`

这一阶段不要自动创建云效任务，也不要自动关单。当前已做到可记录、可查询、可追踪。

### Step 2：把钉钉读取接入 workflow（已完成 P0）

目标：`/workflow/start` 后可读取钉钉文档并推进到 `DOC_READ`。

实现：

- `/workflow/{id}/advance` 在 `CREATED` 状态调用现有 `read_dingtalk_doc`。
- 将返回结果摘要保存到 `context_json`。
- 事件表写入 `doc_read`。

### Step 3：固化 Codex 需求解析协议（已完成 P0，待增强）

目标：Codex 不只是“读懂”，还要把结果以固定 JSON 提交回 Adapter。

实现：

- 定义 `/workflow/{id}/requirement` 入参。
- 保存结构化需求。
- 状态推进到 `REQUIREMENT_PARSED`。
- 返回下一步建议，例如是否创建云效任务。

### Step 4：实现云效流水线绑定 workflow（已完成，Apifox 已验证）

目标：Codex 提交 coding result 后，云效流水线回调能推进 workflow。

实现：

- 云效 success/failure 事件可按 `WORKFLOW_ID` 精确匹配 workflow。
- 状态可推进到 `PIPELINE_SUCCESS`、`PIPELINE_FAILED`。
- 成功后调用现有 Apifox 同步逻辑。
- Apifox 成功后推进到 `APIFOX_SYNCED`。

待增强：

- `PIPELINE_RUNNING` 运行中事件。
- 不带 `WORKFLOW_ID` 时的兜底匹配。

### Step 5：接入 Coding 结果（已完成 P0）

目标：Codex 完成编码后，结果可进入 workflow。

实现：

- `/workflow/{id}/coding-result`
- 保存 branch/commit/MR/test summary。
- 状态推进到 `CODE_SUBMITTED`。

### Step 6：实现云效工作项创建

目标：结构化需求确认后，自动创建云效任务。

实现：

- `POST /workflow/{id}/advance` 在 `REQUIREMENT_PARSED` 状态调用云效 `CreateWorkitemV2`。
- 成功后写入 `yunxiao_task_id`，记录 `yunxiao_workitem_created` 事件。
- 随后记录 `coding_requested` 事件，并把状态推进到 `CODING_REQUESTED`。
- 如果 workflow 已有 `yunxiaoTaskId`，重复 advance 不会重复创建。
- 云效账号鉴权维护在 `adapter_yunxiao_account_config`：正式主链路支持 `acs_ak`，历史 OpenClaw CLI 配置已兼容为 `legacy_token`；多个业务项目维护在 `adapter_yunxiao_project_config`。
- MySQL 已配置时，按 `projectName` / `affectedRepos` / `repoUrl` 解析项目并查表；缺少映射不会静默使用默认项目。
- 如果缺少云效账号鉴权、组织、项目、工作项类型或负责人配置，状态保持 `REQUIREMENT_PARSED`，记录 `yunxiao_workitem_create_failed`。
- 负责人解析顺序：`requirement.assigneeId/assigneeName` -> `adapter_yunxiao_project_member.is_default=1` -> 旧 `adapter_yunxiao_project_config.default_assignee` 兜底；指定负责人找不到时直接失败，避免错派。

待联调：

- 真实云效测试项目创建工作项。
- 云效工作项描述中校验 workflowId、钉钉链接、验收标准。

### Step 7：Apifox 成功后关闭云效任务

目标：完整闭环。

实现：

- Apifox 导入成功后调用云效关闭任务接口。
- 状态推进到 `YUNXIAO_TASK_CLOSED`。
- 写最终事件。

## 6. 推荐优先级

| 优先级 | 事项 | 原因 |
| --- | --- | --- |
| Done | workflow 表和基础接口 | P0 已落地 |
| Done | 钉钉读取接入 workflow | P0 已落地 |
| Done | Codex 结构化需求提交 | P0 已落地，后续增强协议 |
| Done | Coding 结果提交 | P0 已落地 |
| Done | 流水线成功绑定 workflow | Apifox 1-6 手工流程已跑通，终点 `APIFOX_SYNCED` |
| P1 | 云效任务创建真实联调 | 代码和 DB 配置模型已实现，缺真实云效 OpenAPI 配置验证 |
| P1 | 云效任务关闭/回写 | 依赖 Apifox 成功和云效关闭配置 |
| P2 | 失败恢复和人工处理 | 提升稳定性 |
| P3 | Skill/MCP/Plugin 化 | 让 Codex 调用更顺滑 |

## 7. 当前结论

`adapter-mvp` 现在已经完成了“工具底座 + workflow P0 账本 + 流水线成功到 Apifox 同步”的主链路：

- 能被云效调用。
- 能读钉钉文档。
- 能从钉钉 URL 创建 workflow。
- 能把钉钉读取结果、结构化需求、coding 结果写入 workflow。
- 能接流水线事件。
- 能分析失败。
- 能在流水线成功后同步 Apifox，并推进到 `APIFOX_SYNCED`。
- 能记录审计和状态。

但它还不是完整的交付 Workflow Orchestrator。当前最关键的下一步，不再是 P0 账本或流水线绑定，而是把云效工作项的首尾两段接上：

```text
云效工作项创建
云效工作项关闭/回写
```

账本已经有了，接下来要让外部系统按账本推进。

## 8. 云效相关任务拆分

云效这块不要作为一个大任务一次性做完，建议拆成三个可并行但边界清晰的任务：

| 任务 | 文档 | 输入状态 | 输出状态 | 说明 |
| --- | --- | --- | --- | --- |
| 云效 OpenAPI 调研 | [云效 OpenAPI 调研](/Users/jzm/Documents/develop/user-home/workspaces/adapter-mvp/docs/云效开放接口调研方案.md) | 云效官方 OpenAPI | 接口、字段、联调方案 | 已完成，后续实现必须以此为基线 |
| 云效工作项创建 | [云效工作项创建任务](/Users/jzm/Documents/develop/user-home/workspaces/adapter-mvp/docs/云效工作项创建任务.md) | `REQUIREMENT_PARSED` | `YUNXIAO_TASK_CREATED` / `CODING_REQUESTED` | 只负责主动创建云效主工作项 |
| 云效流水线打通 | [云效流水线打通任务](/Users/jzm/Documents/develop/user-home/workspaces/adapter-mvp/docs/云效流水线打通任务.md) | `CODE_SUBMITTED` | `PIPELINE_SUCCESS` / `PIPELINE_FAILED` / `APIFOX_SYNCED` | 只负责流水线事件绑定 workflow 和 Apifox 交接 |
| 云效工作项关闭/回写 | [云效工作项关闭任务](/Users/jzm/Documents/develop/user-home/workspaces/adapter-mvp/docs/云效工作项关闭和回写任务.md) | `APIFOX_SYNCED` | `YUNXIAO_TASK_CLOSED` | 只负责 Apifox 成功后的云效回写和关单 |

推荐实施顺序：

```text
云效工作项创建
  -> 云效工作项关闭/回写
```

工作项关闭依赖 `APIFOX_SYNCED`，现在该状态已能通过 Apifox 手工流程触达，可以作为下一阶段测试入口。
